import {StatusConsumer} from "./status-consumer.enum";

export class Consumer {
  name: string;
  rg: string;
  status: StatusConsumer;
  cpfCnpj: string;
  birthday: number;
  email: string;
  foneTitular: string;
  contato: string;
  foneContato: string;
  // endereco: Endereco;
}
