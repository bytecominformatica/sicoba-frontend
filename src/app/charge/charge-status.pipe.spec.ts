import { ChargeStatusPipe } from './charge-status.pipe';

describe('ChargeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ChargeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
